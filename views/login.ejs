<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link href="/output.css" rel="stylesheet" />
    <script src="/scripts/script.js" defer></script>
    <title>Helper Hub</title>
  </head>
  <body
    class="bg-darkskyblue font-oswald select-none max-h-[calc(100vh-4.25rem)] overflow-x-hidden overflow-Y-hidden"
  >
    <%- include('partials/nav.ejs') %>
    <main>
      <section
        class="flex flex-col justify-center items-center min-h-screen relative"
      >
        <div class="w-full h-screen flex items-center justify-center">
          <img
            class="absolute -z-10 top-[20%] saturate-[.65] drop-shadow-neo-pink"
            src="/images/blob3.svg"
          />
          <img class="absolute -z-10" src="/images/dashed_curl.png" />
          <form
            method="post"
            action="login?type=<%= type %>"
            class="w-full transition-all md:w-1/3"
          >
            <div class="flex font-bold justify-center mt-6">
              <img class="w-72 animate-wiggle1" src="/images/plane.png" />
            </div>
            <h2 class="text-3xl text-center text-black mb-8">
              <% if (type) { %>
                <%= type.charAt(0).toUpperCase() + type.slice(1) %>               
              <% } %>
              Login
            </h2>
            <div class="px-12 pb-10">
              <div class="w-full mb-2">
                <div class="flex items-center">
                  <input
                    type="email"
                    placeholder="Email Address"
                    autocomplete="username"
                    name="email"
                    id="email"
                    class="w-full rounded-[1px] px-3 py-2 text-gray-700 border-2 border-black shadow-neo-sm focus:shadow-neo-md focus:outline-none"
                    autofocus
                    required
                  />
                </div>
              </div>
              <div class="w-full mb-2">
                <div class="flex items-center">
                  <input
                    type="password"
                    placeholder="Password"
                    autocomplete="current-password"
                    name="password"
                    id="password"
                    class="w-full rounded-[1px] px-3 py-2 text-gray-700 border-2 border-black shadow-neo-sm focus:shadow-neo-md focus:outline-none"
                    required
                  />
                </div>
              </div>
              <% if (!v) { %>
              <div id="wrong" class="font-bold text-lg">
                🔥 Wrong email/password
              </div>
              <% } %>
              <button
                type="submit"
                class="py-2 mt-8 bg-teal-400 text-black focus:shadow-neo-md focus:outline-none font-bold text-3xl px-16 border-4 border-black w-full mx-auto shadow-neo-sm hover:shadow-neo-lg rounded-[1px] transition-all -rotate-1 hover:rotate-0 hover:-hue-rotate-90 active:shadow-neo-md"
              >
                Let's Go!
              </button>
            </div>
          </form>
        </div>
      </section>
    </main>
    <script>
      // document.addEventListener("DOMContentLoaded", _ => {
      //   console.log('DOM Loaded');
      //   scrollDown();
      // });
    </script>
  </body>
</html>
